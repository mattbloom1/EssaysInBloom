# Local tooling
.claude/

# OS / editor
.DS_Store
Thumbs.db

# Adobe temp/swap files
*.tmp
~*

# Note: Adobe source files (*.ai, *.indd) in Adobe/ are intentionally tracked.

# Dependencies / build output (for the website, added later)
node_modules/
dist/
build/
.env
.env.local
