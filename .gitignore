# Local tooling
.claude/

# OS / editor
.DS_Store
Thumbs.db

# Adobe temp/swap files
*.tmp
~*

# Note: Adobe source files (*.ai, *.indd) in Adobe/ are intentionally tracked.

# Dependencies / build output (leftovers from the old site; future site too)
node_modules/
dist/
build/
.astro/
.env
.env.local
