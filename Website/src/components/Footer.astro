---
import { withBase } from '../lib/withBase';

const year = new Date().getFullYear();

const nav = [
  { href: withBase('/about/'), label: 'About' },
  { href: withBase('/approach/'), label: 'Approach' },
  { href: withBase('/coaching/'), label: 'Coaching' },
  { href: withBase('/workshops/'), label: 'Workshops' },
  { href: withBase('/testimonials/'), label: 'Testimonials' },
  { href: withBase('/contact/'), label: 'Contact' },
];

const legal = [
  { href: withBase('/terms/'), label: 'Terms of Service' },
  { href: withBase('/privacy/'), label: 'Privacy Policy' },
  { href: withBase('/accessibility/'), label: 'Accessibility' },
];
---

<footer class="footer">
  <div class="container footer__grid">
    <div class="footer__brand">
      {/* Full stacked lockup — includes the "What's Your Story?" tagline */}
      <img src={withBase('/logos/full-cream.svg')} alt="Essays in Bloom — What's Your Story?" width="143" height="175" />
    </div>
    <nav class="footer__col" aria-label="Footer">
      <h3>Explore</h3>
      {nav.map(({ href, label }) => <a class="link" href={href}>{label}</a>)}
    </nav>
    <div class="footer__col">
      <h3>Say hello</h3>
      <a class="link" href="mailto:essaysinbloom@gmail.com">essaysinbloom@gmail.com</a>
      {/* TODO: real social links */}
      <a class="link" href="#">Instagram</a>
      <a class="link" href="#">LinkedIn</a>
    </div>
  </div>
  <div class="container footer__base">
    <p>© {year} Essays in Bloom. All rights reserved.</p>
    <div class="footer__legal">
      {legal.map(({ href, label }) => <a class="link" href={href}>{label}</a>)}
    </div>
  </div>
</footer>

<style>
  .footer {
    background: var(--color-primary);
    color: var(--soft-linen);
    /* no margin-top: the CTA band's bottom padding (or a page's last
       .section padding) provides the space above */
    padding-block: var(--space-12) var(--space-6);
    font-size: var(--text-sm);
  }

  .footer__grid {
    display: grid;
    grid-template-columns: 2fr 1fr 1fr;
    gap: var(--space-8);
    padding-bottom: var(--space-8);
  }

  .footer__brand img {
    height: 104px;
    width: auto;
  }

  .footer__col {
    display: grid;
    gap: var(--space-1);
    justify-items: start;
    align-content: start;
  }

  /* comfortable thumb targets without changing the visual rhythm */
  .footer__col :global(.link) {
    padding-block: 0.4em;
  }

  .footer__col h3 {
    color: var(--light-bronze);
    font-size: var(--text-xs);
    margin-bottom: var(--space-2);
  }

  .footer__base {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-4);
    justify-content: space-between;
    padding-top: var(--space-4);
    font-size: var(--text-xs);
    color: color-mix(in srgb, var(--soft-linen) 75%, transparent);
  }

  .footer__legal {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2) var(--space-6);
  }

  .footer__legal :global(.link),
  .footer__base p {
    padding-block: 0.4em;
  }

  @media (max-width: 720px) {
    /* Brand on top, then the two link columns side by side — half the
       height of the old single-column stack */
    .footer__grid {
      grid-template-columns: 1fr 1fr;
      gap: var(--space-6);
    }

    .footer__brand {
      grid-column: 1 / -1;
    }

    .footer__brand img {
      height: 88px;
    }
  }
</style>
